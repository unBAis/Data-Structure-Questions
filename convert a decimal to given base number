def decimal_to_any_bit(n,base):
    div = 1
    res = ''
    while div != 0 :
        (div,rem) = (n//base , n%base)
        n = div
        res += str(rem)

    fin = ''
    for i in range(len(res)-1,-1,-1):
        fin += res[i]
    return fin

